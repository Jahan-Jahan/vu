import javax.swing.*;

public class EmployeeHome extends JFrame {
    
    public EmployeeHome() {
        setTitle("Employee");
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setSize(500, 500);
        setVisible(true);
    }
}
